import React from 'react';
import Navigation from "../components/Navigation";

const Projects = () => {
    return (
        <div>
            <Navigation/>
            Portfolio works
        </div>
    );
};

export default Projects;
