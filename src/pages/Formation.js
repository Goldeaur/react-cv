import React from 'react';
import Navigation from "../components/Navigation";

const MyComponent = () => {
    return (
        <div>
            <Navigation/>
            Formation works
        </div>
    );
};

export default MyComponent;
